<html>
    <head>
        <title>node js</title>
        <meta charset="utf-8">
    </head>
    <body>
        {{#each users as |user|}}
        <div>
            <img src="/profilepics/{{user.username}}_sm.jpg" alt="">
            <a href="/{{user.username}}">{{user.name.full}}</a>
        </div>
        {{/each}}
    </body>
</html>